# LatticeFlow
LatticeFlow is a project focused on creating a framework  for developing safer, more efficient crash-absorbing structures. Instead of relying on traditional, slow engineering simulations, this framework uses Graph Neural Networks (GNN) to predict physics and Reinforcement Learning (RL) to find the best designs.
